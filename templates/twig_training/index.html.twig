{% extends 'base.html.twig' %}

{% block title %}Hello TwigTrainingController!{% endblock %}

{% block body %}
    
    {% macro input(name, value, type = "text", size = 20) %}
        <input type="{{ type }}" name="{{ name }}" value="{{ value|e }}" size="{{ size }}"/>
    {% endmacro %}
        
    {{ _self.input('nom', 'Nahary') }}

{% endblock %}
